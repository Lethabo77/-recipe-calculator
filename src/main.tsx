import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(</React.St
**Commit message:** `Add React entry point`

---

### **File 3: `src/App.tsx`**
**Filename:** `src/App.tsx`  
**Content:**
```tsx
import RecipeCalculator from "./RecipeCalculator";

function App() {
  return ();
}

export default App;
